// Watchdog removed — diff comparison is automatic between sessions.
// No configuration needed: every time you connect to the same game twice,
// the diff history captures what changed automatically.
export {};
